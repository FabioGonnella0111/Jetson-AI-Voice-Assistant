# Model-free quality checks used by CI. Runtime model/audio packages are
# intentionally excluded so tests can run without a microphone or model files.
numpy>=1.26,<3
httpx>=0.27,<1
pytest>=8.2,<9
ruff>=0.9,<1
tomli>=2,<3; python_version < "3.11"
