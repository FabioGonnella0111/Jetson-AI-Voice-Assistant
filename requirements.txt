# Portable workstation install. Jetson deployments must use
# requirements-jetson.txt after installing the JetPack-compatible backends.
-r requirements-runtime.txt

# Default CPU/desktop inference backends. These are deliberately bounded rather
# than CUDA-pinned; CUDA/JetPack compatibility belongs to the target image.
onnxruntime>=1.17,<2
piper-tts>=1.2,<2
torch>=2.2,<3
